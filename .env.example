# Genius lyrics API (either name works; scripts prefer GENIUS_ACCESS_TOKEN if both are set)
GENIUS_ACCESS_TOKEN=
GENIUS_API_TOKEN=your_genius_token_here

# Spotify Web API (Phase 4b — client credentials only; never commit secrets)
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
